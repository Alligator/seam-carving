{
  "name": "seam-carving",
  "version": "0.0.1",
  "main": "build/index.js",
  "files": ["build/*"],
  "scripts": {
    "build": "tsc",
    "build-and-run": "tsc && cd canvas-test && node index.js",
    "test": "cd test && npm start"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "typescript": "^4.2.2"
  },
 "dependencies": {
  }
}
